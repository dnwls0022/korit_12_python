# md파일은 vs코드사용

md파일작성은 code에서 
python코드는 pycharm에서 


1/30 

# pyhton start
print ()
''
함수로실행시킴

```py
def turn_right():
    turn_left()
    turn_left()
    turn_left()
    
def jump():
  
    move()
    turn_left()
    move()
    turn_right()
    move()
    turn_right()
    move()
    turn_left()
    
    메서드 반복되는코드들을 하나의함수로묶어놓은것


    jump()
```

```python
def turn_right(): 
    turn_left()
    turn_left()
    turn_left()



# goal에 도착하면 멈춰야한다.
while not at_goal():
    if wall_on_right() and not wall_in_front()
        turn_left()
    elif wall_on_right() and front_is_clear()
        move()
    elif right_is_clear()
        turn_right()
        move()

        
```
## ch03 indexing 
if number%2==0:
    print('마지막숫자는 '+number+"이며짝수입니다.")
elif number%2!=0:
    print('마지막숫자는 홀수입니다')        
    import random
word_list = ['apple','banana','mango']
# chosen_word 에 word_list 에있는 단어중하나를 고른다
chosen_word = random.choice(word_list)
# 사용자가 고른단어를 입력받도록 input을쓴다 그 단어가 소문자가되도록 lower를 적용한다.
guessed_word = (input('원하시는 단어를 입력하세요>>>').lower())

for letter  in chosen_word:
    if letter == guessed_word:
        print('정답')
    else:
        print('오답')

